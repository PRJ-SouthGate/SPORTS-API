<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Player Not Found</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500&display=swap" rel="stylesheet"> <!-- Noto Sans KR 폰트 추가 -->
    <style>
        body {
            font-family: 'Noto Sans KR', sans-serif; /* Noto Sans KR로 폰트 변경 */
            background-color: #edf2f7; /* 밝은 회색 배경 */
            text-align: center;
            padding: 50px;
        }

        h1 {
            color: #2b6cb0; /* 밝은 파란색 */
            font-size: 28px;
        }

        p {
            color: #444; /* 어두운 회색 */
            font-size: 18px;
        }

        form {
            margin-top: 20px;
        }

        input[type="hidden"] {
            display: none;
        }

        button {
            background-color: #2b6cb0; /* 밝은 파란색 */
            color: white;
            border: none;
            border-radius: 5px;
            padding: 10px 20px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #2c5282; /* 어두운 파란색 */
        }

        .message {
            font-size: 20px;
            color: #2b6cb0; /* 밝은 파란색 */
            margin-top: 10px;
            display: inline-block;
            vertical-align: top;
            padding-left: 20px;
        }
    </style>
</head>
<body>
    <h1>😢 Player Not Found</h1>
    <p>The statistics for <%= playerName %> could not be found.</p>
    <form action="/soccer/updatePlayerStats" method="post" style="display: inline-block;">
        <input type="hidden" name="playerName" value="<%= playerName %>" />
        <input type="hidden" name="leagueId" value="<%= leagueId %>" />
        <input type="hidden" name="existence" value="<%= existence %>" />
        <button type="submit">Okay</button>
    </form>
    <span class="message">Do it Yourself!</span>
</body>
</html>